# <Use case title>

<!--
Copy this file to candidates/NNN-your-slug/README.md and fill it in.
Delete the comments as you go. Every section is required except "Regenerate the data".
Keep it plain: short sentences, name what a step does, say what a reader has to change.
-->

One or two sentences: what this pipeline does, on what material, for what research
question. Link the project if it has a page.

## What you need

- **Session:** JupyterHub / batch job / interactive shell. GPU or CPU.
- **Resources:** roughly how much GPU memory, RAM, and disk. Say if it will not run
  without a GPU.
- **Software:** what the setup step installs, and what you are expected to have already.

## Quickstart

```bash
# The shortest path from a fresh session to a first result.
```

## What's inside

| File | What it is |
|---|---|
| `pipeline.ipynb` | the notebook you run |
| `helpers.py` | code the notebook imports |
| `data/` | the sample slice, see the licence below |

## Settings to change before you run

List every value that points at the author's setup — paths, GPU counts, worker counts —
with what to set it to. If a reader has to change one thing, say which.

## What it produces

Where the output lands and in what format.

## Data

Where the material comes from, what the sample here is a slice of, and how to get the
full set.

## Regenerate the data

<!-- Only if the sample is generated. Give the command. -->

## Licence

State the licence for the code and the exact Creative Commons licence for the material,
and name the source and licence of any third-party material. See the repository README.

## Credits

Tools, models, and data providers, with links.
